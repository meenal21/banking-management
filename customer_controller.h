#ifndef CUSTOMER_CONTROLLER_H
#define CUSTOMER_CONTROLLER_H

#include "customer.h"

int customer_controller(){
    // first login the customer-

    // if yes then show the menu, else return -1
    // once logged in then while loop until exit
    // on exit - logout - and sever the connection
    // on logout - exit - go back to the menu

    printf("Enter userid");
    
    loggedin = login_customer()
}